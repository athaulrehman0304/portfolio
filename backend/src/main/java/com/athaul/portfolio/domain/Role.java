package com.athaul.portfolio.domain;

/**
 * Authorization roles supported by the application. Only a single
 * administrative role exists; there is no public registration.
 */
public enum Role {
    ADMIN
}
